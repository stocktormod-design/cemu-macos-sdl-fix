# Archive

Older experiments (e.g. DSU bridge) kept for reference. **Not required** for the SDL fix — use the main [README](../README.md).

`CEMU_CONTROLLER_SETUP.md` in the repo root is a legacy Norwegian note about the DSU approach; the recommended fix is `install_cemu_sdl_fix.sh` / `CemuSDLFix.app`.
